const bidsData = require('./bids');
const commentsData = require('./comments');
const listingsData = require('./listings');
const usersData = require('./users');

module.exports = {
    bids: bidsData,
    comments: commentsData,
    listings: listingsData,
    users: usersData
};